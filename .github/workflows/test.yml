on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          cargo install just
          just init
          just db-migrate
      - uses: will-lynas/rust-check-ci@v0.2.0
